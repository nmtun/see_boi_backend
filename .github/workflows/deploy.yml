name: Deploy See Boi BE

on:
  push:
    branches: ["main"]
  # workflow_dispatch:   # bật nếu muốn cho phép deploy thủ công

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🟢 Checkout source
        uses: actions/checkout@v3

      - name: 🔐 SSH Deploy to VPS
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          password: ${{ secrets.VPS_PASSWORD }}

          script: |
            echo "📁 Chuyển đến thư mục BE"
            cd /opt/see_boi_backend

            echo "📥 Pull code mới nhất"
            git pull origin main

            echo "🔓 Cấp quyền thực thi"
            chmod +x build.sh

            echo "🚀 Chạy script build"
            ./build.sh
