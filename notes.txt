Cách chạy dự án

# Cài đặt dependencies
    cd backend
    npm install

# Tạo file .env
    DB_HOST=localhost
    DB_PORT=3307
    DB_NAME=see_boi
    DB_USER=root
    DB_PASSWORD=123456
    PORT=8080

# Chạy container MySQL:
    docker-compose up -d

# Chạy project 
    nest start --watch     

# Mở cmd để sử dụng mysql thông qua docker exec:
    docker exec -it see_boi_mysql mysql -u root -p
    pass: 123456


# Để tạo module chạy câu lệnh sau: nest g resource modules/<tên module> (chọn rest + gen crud)