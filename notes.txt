# Cài đặt dependencies:
    cd backend
    npm install

# Tạo file .env
    DATABASE_URL="postgresql://admin:admin123@localhost:5432/mydb?schema=public"
    JWT_SECRET="bi_mat_khong_bat_mi"
    JWT_EXPIRATION="24h"

# Chạy container MySQL:
    docker-compose up -d

# Chạy migration (dùng khi muốn tạo mới migration):
    npx prisma migrate dev --name ten_migration_cua_ban

# Chạy migration đã có và generate lại:
    npx prisma migrate deploy
    npx prisma generate

# Import user qua seed-user:
    npx ts-node seed-users.ts

# Import dữ liệu từ seed.sql:
    Get-Content seed.sql | docker exec -i my-postgresae psql -U admin -d mydb

# Chạy project:
    nest start --watch     

# Mở cmd để sử dụng mysql thông qua docker exec:
    docker exec -it my-postgresae psql -U admin -d mydb

# Để tạo module chạy câu lệnh sau:
    nest g resource modules/<tên module> (chọn rest + gen crud)
